/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BinaryPipe } from './binary.pipe';

describe('BinaryPipe', () => {
  it('create an instance', () => {
    const pipe = new BinaryPipe();
    expect(pipe).toBeTruthy();
  });
});
